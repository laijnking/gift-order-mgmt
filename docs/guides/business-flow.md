# 礼品订单管理系统 - 核心业务流数据流转

更新时间：2026-04-29

## 1. 业务流总览

### 1.1 完整业务流程

```
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                                    客户下单                                            │
│                          (微信/表格/其他渠道)                                          │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
                                         │
                                         ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                               订单录入模块 (P0)                                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ Excel导入   │  │ 表头解析   │  │ 商品匹配   │  │ 发货方匹配  │              │
│  │ (整张sheet)│  │ (指纹匹配) │  │ (SKU映射)  │  │ (库存+成本) │              │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘              │
│                              │                                                          │
│         ┌────────────────────┼────────────────────┐                                 │
│         ▼                    ▼                    ▼                                 │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐                       │
│  │ 商品档案    │      │ 库存档案    │      │ 发货方档案  │                       │
│  │ (products) │      │ (stocks)   │      │ (shippers) │                       │
│  └─────────────┘      └─────────────┘      └─────────────┘                       │
│                              │                                                          │
│         ┌────────────────────┼────────────────────┐                                 │
│         ▼                    ▼                    ▼                                 │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐                       │
│  │ 历史成本库  │      │ 列映射表    │      │ SKU映射表   │                       │
│  │ (派发写入) │      │ (feedback_ │      │ (supplier_ │                       │
│  │            │      │  export_   │      │  product_  │                       │
│  │            │      │  headers)   │      │  code)      │                       │
│  └─────────────┘      └─────────────┘      └─────────────┘                       │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
                                         │
                                         ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                               订单中心模块 (P0)                                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ 订单列表    │  │ 订单详情   │  │ 状态流转   │  │ 批量派发   │              │
│  │ (含批次摘要)│  │ (含商品快照)│  │            │  │            │              │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘              │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
              │                                                    │
              │                                                    │
              ▼                                                    ▼
┌─────────────────────────────┐          ┌─────────────────────────────────────────┐
│      发货通知单模块 (P1)    │          │         历史成本库模块 (P1)              │
│  ┌─────────────┐           │          │  ┌─────────────┐                        │
│  │ 按发货方导出 │           │          │  │ 派发写入   │                        │
│  │ (三种模式)   │           │          │  └─────────────┘                        │
│  └─────────────┘           │          │  ┌─────────────┐                        │
│  ┌─────────────┐           │          │  │ 回单补全   │                        │
│  │ 派发记录     │───────────┼──────────┼──│ (快递信息)  │                        │
│  └─────────────┘           │          │  └─────────────┘                        │
│  ┌─────────────┐           │          │  ┌─────────────┐                        │
│  │ 导出记录    │           │          │  │ 费用录入   │                        │
│  │ (持久化)    │           │          │  │ (分摊)     │                        │
│  └─────────────┘           │          │  └─────────────┘                        │
└─────────────────────────────┘          └─────────────────────────────────────────┘
              │                                       │
              │                                       │
              ▼                                       ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                               物流回单模块 (P1)                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ 回单导入    │  │ 自动匹配   │  │ 冲突检测   │  │ 复核/确认  │              │
│  │            │  │ (多候选→冲突)│  │            │  │            │              │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘              │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
              │                                                    │
              │                                                    │
              ▼                                                    ▼
┌─────────────────────────────────┐  ┌─────────────────────────────────────────────────┐
│       导出记录模块 (P1)          │  │          预警中心模块 (P1)                     │
│  ┌─────────────┐               │  │  ┌─────────────┐  ┌─────────────┐            │
│  │ 发货通知导出 │               │  │  │ 规则执行器  │  │ 预警记录    │            │
│  │ (按发货方)   │               │  │  │ (新增/复用/ │  │ (新建→已处理│            │
│  └─────────────┘               │  │  │  自动关闭)  │  │  →自动关闭) │            │
│  ┌─────────────┐               │  │  └─────────────┘  └─────────────┘            │
│  │ 客户反馈导出 │               │  └─────────────────────────────────────────────────┘
│  │ (客户原始列名)│               │
│  └─────────────┘               │
│  ┌─────────────┐               │
│  │ 文件持久化   │               │
│  │ (本地/S3)   │               │
│  └─────────────┘               │
└─────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                               数据报表模块 (P1)                                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ 订单统计    │  │ 销售业绩   │  │ 发货方分析  │  │ 回单时效   │              │
│  │ (含回单阶段)│  │            │  │            │  │ (avgDispatch│              │
│  │            │  │            │  │            │  │  Days)      │              │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘              │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 数据流转详细说明

### 2.1 订单录入 → 档案管理

#### 2.1.1 选择客户 → 自动联动业务员/跟单员

**交互场景**：选择客户后，自动按 ID 填充客户档案中绑定的业务员和跟单员。

**数据流转**：

```
1. 用户选择客户
2. 系统查询 customers 表，获取：
   - salesperson_id, salesperson_name: 来自 customers 表的绑定关系
   - order_taker_id, order_taker_name: 同上
3. 订单录入表单自动填充对应字段
```

#### 2.1.2 Excel 解析 → 列映射保存

**数据流转**：

```
1. 用户上传 Excel，选择客户
2. 系统自动加载历史映射（按表头指纹 SHA256 精确匹配）
3. 用户可手动调整映射
4. 保存映射时：
   - 写入 mapping_config（列索引 → 系统字段名）
   - 写入 source_headers（原始表头数组）
   - 写入 header_fingerprint（SHA256 前 16 位）
   - 写入 template_signature（映射 + 表头行 + 指纹）
   - 写入 feedback_export_headers（"客户列名" → "系统字段名"）
```

---

### 2.2 订单录入 → 库存管理

#### 2.2.1 商品匹配后查询库存

**数据流转**：

```
1. 商品匹配成功后，获取 productId
2. 查询 stocks 表，获取所有有库存的发货方
3. 按发货方分组，返回库存信息
4. 如果库存 ≤ 2 台，触发尾货预警
```

#### 2.2.2 库存扣减

**触发时机**：dispatch_only / dispatch_with_persistence 模式派发时

**数据流转**：

```
1. 订单派发后，更新 stocks 表
   - quantity = quantity - order_quantity
2. 写入 stock_versions 表记录变更
   - change_type: "order"
   - change_quantity: -order_quantity
3. 如果库存扣减到 ≤ 2 台，触发预警
```

---

### 2.3 订单提交 → 订单中心

#### 2.3.1 订单提交

**数据流转**：

```
1. 用户确认订单信息，点击提交
2. 系统校验：
   - 必填字段：收货人、收货电话、收货地址
   - 商品信息：至少有一条商品明细
3. 生成系统订单号：SYS-YYYYMMDD-XXXX-TIMESTAMP
4. 写入 orders 表（items 为 JSONB 数组）
5. 生成导入批次号（import_batch）
6. 返回提交结果（含创建数量、跳过数量、系统订单号）
```

#### 2.3.2 订单号生成规则

**格式**：`SYS-{日期}-{序号}-{时间戳随机码}`

**示例**：`SYS-20260416-0001-M1ABC2D3`

---

### 2.4 订单派发 → 发货通知单

#### 2.4.1 三种派发模式

| dispatchMode | 效果 | 副作用 |
|-------------|------|--------|
| `preview` | 仅生成预览内容 | 无任何副作用 |
| `dispatch_only` | 派发订单 | 扣减库存、写入派发记录、写入成本记录 |
| `dispatch_with_persistence` | 派发 + 留痕 | 包含上述所有 + 写入导出记录、文件持久化 |

#### 2.4.2 发货方商品编码快照

**数据流转**：

```
1. 订单派发时，根据 orders.items 中的 product_id 和 supplier_id
2. 调用 findSupplierProductMapping(client, supplierId, productId)
3. 查询 product_mappings 表获取发货方商品编码/名称/规格
4. 若查询到映射 → 使用映射中的 supplierProductCode/Name/Spec
   若无映射 → fallback 到 stocks 表的 product_code / product_name
5. 写入 dispatch_records.items[]
6. 导出时直接从 dispatch_items 读取，不再查询 product_mappings
```

---

### 2.5 发货通知单 → 物流回单

#### 2.5.1 发货方回传

**数据流转**：

```
1. 用户上传回单 Excel（包含订单号、快递公司、快递单号）
2. 系统按匹配规则匹配订单：
   - 优先级1：快递单号精确匹配
   - 优先级2：订单号 + 收货人手机号匹配
   - 优先级3：订单号 + 收货人姓名匹配
   - 优先级4：收货人手机号 + 商品名称匹配
3. 匹配结果：
   - 单一匹配 → 自动匹配成功（auto_matched）
   - 多候选匹配 → 冲突（conflict），进入复核池
   - 无匹配 → 未匹配（unmatched），进入复核池
4. 用户可在复核池中处理冲突/未匹配记录
5. 批量确认后更新订单状态
```

---

### 2.6 回单 → 历史成本库

#### 2.6.1 回单后更新成本信息

**数据流转**：

```
1. 回单匹配成功后（auto_matched / manual_matched）
2. 查询 order_cost_history 表
3. 更新：
   - express_company: 快递公司
   - tracking_no: 快递单号
   - returned_date: 回单日期
   - total_amount: total_cost + express_fee + other_fee
4. 更新 orders.returned_at（回单时效报表依赖此字段）
```

---

## 3. 状态流转图

### 3.1 订单状态流转

```
┌────────────────────────────────────────────────────────────────────────────────────────────┐
│                                                                                        │
│    ┌─────────┐    派发    ┌──────────┐    导出发货通知  ┌──────────┐    回单    ┌─────────┐    完成    │
───▶│ pending │ ────────▶ │ assigned │ ───────────────▶ │  notified │ ────────▶ │ returned │ ──────▶ │completed│
│ (待派发) │            │ (已派发) │                  │ (通知发货) │            │ (已回单) │          │(已完成) │
│    └────┬────┘            └────┬─────┘                  └────┬─────┘            └────┬────┘          └─────────┘
│         │                       │                            │                       │
│         │ 取消                  │ 部分回单                   │ 部分回单              │
│         ▼                       ▼                            ▼                       │
│    ┌───────────┐          ┌────────────────┐          ┌────────────────┐            │
│    │ cancelled │          │partial_returned│◀─────────│partial_returned│◀──────────┘
│    │ (已取消)  │          │  (部分回单)    │   回单   │   (部分回单)   │    回单
│    └───────────┘          └────────────────┘          └────────────────┘
```

### 3.2 回单匹配状态流转

```
┌──────────────────────────────────────────────────────────────────────────┐
│                                                                          │
│    ┌─────────┐    自动匹配   ┌──────────┐   批量确认   ┌──────────┐    │
───▶│ pending │ ───────────▶ │auto_matched│ ────────▶ │ confirmed │        │
│(待匹配) │                │(自动匹配)  │            │ (已确认)  │        │
│    └────┬────┘                └─────┬──────┘            └──────────┘        │
│         │                           │                                          │
│         │ 自动匹配(多候选)        │                                          │
│         ▼                           ▼                                          │
│    ┌──────────┐   人工处理    ┌────────────┐                              │
│    │ conflict │ ────────────▶ │manual_matched│ ──────▶ 确认 ──▶ 确认     │
│    │ (冲突)   │               │(手动匹配)   │                              │
│    └──────────┘               └────────────┘                              │
│         │                                                                │
│         │ 人工处理                                                         │
│         ▼                                                                │
│    ┌───────────┐                                                         │
│    │unmatched │                                                         │
│    │(未匹配)  │                                                         │
│    └───────────┘                                                         │
```

---

## 4. 模块关联表

| 交互点 | 模块A | 模块B | 数据交互说明 |
|--------|-------|-------|-------------|
| 1 | 订单录入 | 库存管理 | 订单录入时查询库存和发货方匹配 |
| 2 | 订单录入 | 历史成本库 | 派发订单时创建成本记录（第一阶段） |
| 3 | 订单录入 | 订单中心 | 提交订单后跳转到订单中心 |
| 4 | 订单中心 | 发货通知单 | 订单派发后生成发货通知单 |
| 5 | 发货通知单 | 物流回单 | 发货方发货后回传快递单号 |
| 6 | 发货通知单 | 导出记录 | 派发+留痕时写入导出记录和文件持久化 |
| 7 | 物流回单 | 历史成本库 | 回单后更新成本记录快递信息（第二阶段） |
| 8 | 物流回单 | 订单中心 | 回单后更新订单状态为已回单 |
| 9 | 物流回单 | 导出记录 | 客户反馈导出时使用客户原始列名 |
| 10 | 档案管理 | 订单录入 | 订单录入时关联客户、商品数据 |
| 11 | 档案管理 | 订单中心 | 订单详情显示客户、商品档案信息 |
| 12 | 档案管理 | 数据报表 | 报表数据来源于档案和订单数据 |
| 13 | 库存管理 | 预警中心 | 库存变化触发预警规则执行 |
| 14 | 预警中心 | 订单中心 | 预警记录在订单中心侧栏显示 |

---

## 5. 相关文档

| 文档 | 说明 |
|------|------|
| [订单管理指引](./orders) | 订单列表、筛选、派发、详情查看 |
| [订单解析指引](./order-parse) | Excel导入、表头解析、商品匹配 |
| [发货导出指引](./shipping-export) | 发货通知单生成、派发模式 |
| [回单管理指引](./return-receipt) | 回单导入、自动匹配、冲突处理 |
| [反馈导出指引](./feedback-export) | 客户反馈导出、客户原始列名 |
| [库存管理指引](./stocks) | 库存查询、版本历史、价格历史 |
| [预警中心指引](./alerts) | 预警规则、预警记录处理 |

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0.0 | 2026-04-29 | 初始版本 |
